import HomePage from "./home/page"

export default function RootPage() {
  // You can add logic here to redirect based on authentication status
  // For now, we'll just render the home page
  return <HomePage />
}
